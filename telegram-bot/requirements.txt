flask==3.0.0
requests==2.31.0
gunicorn==21.2.0
pyotp==2.9.0
# Kotak Neo Trade API SDK — only actually contacted when LIVE_TRADING=true,
# but the package must be installed for that code path to work.
git+https://github.com/Kotak-Neo/Kotak-neo-api-v2.git@v2.0.2#egg=neo_api_client
